{
  "schemaVersion": 1,
  "id": "safeloop-extract-tokenCounter-helper",
  "description": "Drive SafeRefactorLoop on an extract-method refactor inside TokenCounter.splitByStatementsPreservingSignature. The 5-line header-builder block (annotations + declaration + opening brace) is a clean local extraction with no cross-class callers; should produce a SAFE verdict. Earlier version of this fixture targeted splitAtLineBoundaries — that helper has since been extracted in source, so this fixture was rewritten to a still-pending opportunity in the same file.",
  "mode": "safeloop",
  "query": "In com.smolnij.chunker.tokenizer.TokenCounter.splitByStatementsPreservingSignature, extract the method-header construction block (annotations, declaration, opening brace) into a private helper named buildMethodHeader that takes a MethodDeclaration and returns the header String.",
  "topK": 8,
  "gold": {
    "anchor": "com.smolnij.chunker.tokenizer.TokenCounter#splitByStatementsPreservingSignature(MethodDeclaration, BlockStmt, String)",
    "relevant": [
      "com.smolnij.chunker.tokenizer.TokenCounter#splitByStatementsPreservingSignature(MethodDeclaration, BlockStmt, String)"
    ]
  },
  "expected": {
    "analyzer": "SAFE",
    "compile": null,
    "tests": []
  },
  "tags": ["self-eval", "safeloop", "extract-method"],
  "timeoutSeconds": 600
}
